package org.nayan.java8_1;

public class Java8QuestionsOnly {

    /*
     =============================================================
                        JAVA 8 STREAM PROGRAMMING QUESTIONS
     =============================================================

     Q1. Find the list of students whose rank is between 50 and 100.

     Q2. Find the students who stay in Karnataka and sort them by their names:
            - ascending order
            - descending order

     Q3. Find all department names (distinct).

     Q4. Find all the contact numbers using flatMap.

     Q5. Group the students by department names.

     Q6. Find the department having the maximum number of students.

     Q7. Find the average age of male and female students.

     Q8. Find the highest (best) rank in each department.

     Q9. Find the student who has the second rank:
            - if rank == 2 exists
            - otherwise, second element after sorting by rank

     Q10. Find the highest (best) rank in each department and
          then sort departments according to that rank.

     =============================================================
                      ARRAY & GENERAL JAVA 8 QUESTIONS
     =============================================================

     Q11. Find the second highest number in an int array using Java 8:
            - using distinct + sorted + skip
            - using max + filter + max
            - using sorted + limit + skip

     Q12. Difference between comparing(...) and comparing(...).reversed()
          when sorting.

     Q13. How to perform case-sensitive and case-insensitive filtering
          in streams while comparing strings (e.g., city = Karnataka).

     =============================================================

     (Add more questions here if needed)
     =============================================================
     */

    public static void main(String[] args) {
        // This class only contains questions in comments.
    }
}
